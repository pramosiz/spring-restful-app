server:
    port: 8003
spring:
#   profiles:
#     active: @spring.profiles.active@
    application:
        name: bike-service
    cloud:
        config:
            enabled: true
            uri: http://${CONFIG_SERVER_URI:config-service}:${CONFIG_SERVER_PORT:8888}
