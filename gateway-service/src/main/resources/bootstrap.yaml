spring:
  application:
    name: gateway-service
    description: App for Gateway Service
  cloud:
    config:
      enabled: true
      uri: http://${CONFIG_SERVER_URI:localhost}:${CONFIG_SERVER_PORT:8888}
