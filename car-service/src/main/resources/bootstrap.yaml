server:
    port: 8002
spring:
#   profiles:
#     active: @spring.profiles.active@
    application:
        name: car-service
    cloud:
        config:
            enabled: true
            uri: http://${CONFIG_SERVER_URI:config-service}:${CONFIG_SERVER_PORT:8888}
