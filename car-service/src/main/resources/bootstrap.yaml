spring:
#   profiles:
#     active: @spring.profiles.active@
    application:
        name: car-service
        description: Servlet RESTful App focus on Car Domain
    cloud:
        config:
            enabled: true
            uri: http://${CONFIG_SERVER_URI:localhost}:${CONFIG_SERVER_PORT:8888}
