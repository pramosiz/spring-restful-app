server:
   port: 8001
spring:
   # profiles:
   #    active: dev
   application:
      name: user-service
   cloud:
      config:
         enabled: true
         uri: http://${CONFIG_SERVER_URI:config-service}:${CONFIG_SERVER_PORT:8888}
