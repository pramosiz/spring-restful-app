server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          default-label: develop-servlet
          uri: https://github.com/pramosiz/spring-restful-app
          search-paths: additional-resources/config-data
          
  application:
    name: @project.name@
    version: @project.version@
    description: @project.description@